# import rules
# from . import predicates

# rules.add_rule('is_ceo', predicates.is_ceo)
# rules.add_rule('is_manager', predicates.is_manager)

# rules.add_rule('create_new_user', predicates.create_new_user)
# rules.add_rule('activate_deactivate_personnel_account', predicates.activate_deactivate_personnel_account)

# rules.add_rule('view_personnel_index', predicates.view_personnel_index)
# rules.add_rule('view_group_index', predicates.view_group_index)
# rules.add_rule('view_permission_index', predicates.view_permissions_index)
# rules.add_rule('permission_grant', predicates.permission_grant)
# rules.add_rule('permission_revoke', predicates.permission_revoke)
# rules.add_rule('group_add_personnel', predicates.group_add_personnel)
# rules.add_rule('group_remove_personnel', predicates.group_remove_personnel)
# rules.add_rule('grant_multiple_permissions', predicates.grant_multiple_permissions)
